#!/usr/bin/env node

var ReverseTunnelServer = require("../lib/node-tunnel").ReverseTunnelServer;

var server = new ReverseTunnelServer();
var parts = process.argv[2].split(":", 2);
server.listen(parts[1], parts[0]);
